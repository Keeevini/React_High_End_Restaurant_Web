import React from 'react';

import { SubHeading, MenuItem } from '../../components';
import { images, data } from '../../constants';
import './Chef.css';

const Chef = () => (
	<div>
		Chef
	</div>
);

export default Chef;